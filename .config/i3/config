exec xrandr --output HDMI-A-0 --right-of DisplayPort-1 --output DisplayPort-1 --mode 2560x1440 --rate 165 --primary 

workspace 9 output HDMI-A-0

font TerminessNerdFont:size=12:hintstyle=hintslight;2

exec xrandr --output DisplayPort-0 --mode 2560x1440 --rate 165 --same-as DisplayPort-1 
exec "setxkbmap -option 'grp:win_space_toggle' -layout us,ua"

#exec --no-startup-id pipewire
#exec --no-startup-id wireplumber
exec killall flamestot
exec killall clipmenud
exec flameshot
exec clipmenud

exec nitrogen --restore
exec xcompmgr 
#exec killall pipewire wireplumber pipewire-pulse

for_window [class="avhdl_core.exe"] floating enable
exec --no-startup-id systemctl --user reload-or-restart pipewire pipewire-pulse
exec --no-startup-id systemctl --user reload-or-restart wireplumber
exec --no-startup-id dbus-update-activation-environment --systemd DBUS_SESSION_BUS_ADDRESS DISPLAY XAUTHORITY


exec_always --no-startup-id sh ~/.config/i3/startpolybar.sh

set $mod Mod4

floating_modifier $mod

bindsym $mod+n exec kitty
bindsym $mod+j exec firefox
bindsym $mod+q kill
bindsym $mod+d exec --no-startup-id dmenu_run
bindsym $mod+y exec pear-desktop

bindsym $mod+F5 exec nitrogen --class floating
bindsym $mod+r exec rofi -theme arc-rofi -show run
bindsym $mod+x exec rofi -theme arc-rofi -show window

for_window [class="floating"] floating enabled move position center
for_window [class="com.github.th_ch.youtube_music"] floating enable move position center
for_window [class="bad_apple"] floating enable 

bindsym $mod+Print exec --no-startup-id flameshot screen -n 0 -p ~/Pictures -c
bindsym Print exec --no-startup-id flameshot gui -c -p ~/Pictures
bindsym $mod+c exec clipmenu

bindsym $mod+g fullscreen toggle

bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

bindsym $mod+h split h
bindsym $mod+v split v

bindsym $mod+t layout tabbed
bindsym $mod+s layout toggle split 
bindsym $mod+p floating toggle

#switching workspaces
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10

bindsym $mod+button4 workspace prev
bindsym $mod+button5 workspace next

bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart

bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +1% && pkill -RTMIN+1 i3blocks 
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -1% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
bindsym XF86AudioPlay, exec playerctl play-pause

bindsym XF86AudioNext exec playerctl next 
bindsym XF86AudioPrev exec playerctl previous

bindsym $mod+Shift+h exec --no-startup-id xdotool mousemove_relative -- -20 0
bindsym $mod+Shift+l exec --no-startup-id xdotool mousemove_relative -- 20 0
bindsym $mod+Shift+k exec --no-startup-id xdotool mousemove_relative -- 0 -20
bindsym $mod+Shift+j exec --no-startup-id xdotool mousemove_relative -- 0 20

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id sh ~/.config/i3/i3exit.sh lock, mode "default"
    bindsym e exec --no-startup-id sh ~/.config/i3/i3exit.sh logout, mode "default"
    bindsym s exec --no-startup-id sh ~/.config/i3/i3exit.sh suspend, mode "default"
    bindsym h exec --no-startup-id sh ~/.config/i3/i3exit.sh hibernate, mode "default"
    bindsym r exec --no-startup-id sh ~/.config/i3/i3exit.sh reboot, mode "default"
    bindsym Shift+s exec --no-startup-id i3exit shutdown, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+Escape exec sh ~/.config/i3/i3exit.sh

for_window [class="Wine"] floating enable
for_window [class="Wine"] border normal
gaps inner 5px
new_window 1pixel
default_border pixel 
focus_follows_mouse yes
mouse_warping output
